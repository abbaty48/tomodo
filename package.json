{
  "name": "tomodo",
  "version": "1.0.0",
  "description": "A web application for downloading torrent movies base on yts.mx api",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "web-dev-server --watch --node-resolve --open --esbuild-target auto"
  },
  "keywords": [
    "torrent",
    "movies",
    "downloader"
  ],
  "author": "abbatyya",
  "license": "ISC",
  "dependencies": {
    "lit-html": "^3.2.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.2"
  }
}
